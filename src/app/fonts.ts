import { Noto_Serif_JP, Poppins } from 'next/font/google';

export const poppins = Poppins({ subsets: ['latin'], weight: '400' });
export const noto900 = Noto_Serif_JP({ subsets: ['latin'], weight: '900' });
